Alright, so I think instead of manually making an uber shader, I should implement a C++ system that takes a GLSL file, reads a specific function with a specific name, registered with the planet manager of that specific planet/body, and then adds it to the uber shader. The uber shader then needs to go through if() functions, since switches don't exactly allow for continuous switching