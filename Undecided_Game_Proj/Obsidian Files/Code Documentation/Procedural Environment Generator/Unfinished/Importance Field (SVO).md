[[Godot GDScript]]

Dirty system; only used for persistent caches (unlikely to be implemented soon). Checks if the probe hits a modified chunk; reconstructs said chunk and feeds it back to the system.